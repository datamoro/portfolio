// Basic interactions can go here
console.log("Welcome to Caiom's Portfolio");

// Optional: Add scroll reveal logic if we want more complex animations than CSS keyframes
document.addEventListener('DOMContentLoaded', () => {
    // Future enhancements
});
